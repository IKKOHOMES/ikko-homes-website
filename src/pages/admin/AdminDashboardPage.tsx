import { useEffect,useState } from 'react'; import { getDashboardSummary,type DashboardSummary } from '../../lib/admin-api';
export function AdminDashboardPage(){const [s,setS]=useState<DashboardSummary>();useEffect(()=>{void getDashboardSummary().then(setS)},[]);const cards=[['New orders',s?.newOrders],['Quote pending',s?.quotePending],['Active products',s?.activeProducts],['Active projects',s?.activeProjects],['Published posts',s?.publishedPosts]];return <section className="admin-dashboard"><p className="eyebrow">IKKO Homes internal</p><h1>Dashboard</h1><div className="dashboard-cards">{cards.map(([l,n])=><article key={String(l)}><b>{n??'—'}</b><span>{l}</span></article>)}</div></section>}
